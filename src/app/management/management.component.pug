kof-claims(*ngIf='isFullViewMode')
.x_panel
    ul.nav.nav-tabs.bar_tabs(*ngIf='isFullViewMode')
        li.hand-cursor([routerLink]="['shops']", [routerLinkActive]="['active']")
            a Магазины
        li.hand-cursor([routerLink]="['contracts']", [routerLinkActive]="['active']")
            a Контракты
    .tab-content
        router-outlet
