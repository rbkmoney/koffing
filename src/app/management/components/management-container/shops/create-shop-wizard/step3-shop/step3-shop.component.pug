.actionBar
    .text-center
        a.btn.btn-primary((click)="stepBackward()") Назад
        a.btn.btn-success.buttonDisabled Вперёд
        a.btn.btn-default(
            (click)="finalizeWizard()",
            [ngClass]="{'buttonDisabled': isShopFieldsReady === false}"
        ) Завершить
.ln_solid
.tile
    kof-add-shop(
        [shopFields]="wizardArgs.shopFields",
        (readyStateChange)="shopFieldsReady($event)"
    )
.actionBar
    .text-center
        a.btn.btn-primary((click)="stepBackward()") Назад
        a.btn.btn-success.buttonDisabled Вперёд
        a.btn.btn-default(
            (click)="finalizeWizard()",
            [ngClass]="{'buttonDisabled': isShopFieldsReady === false}"
        ) Завершить