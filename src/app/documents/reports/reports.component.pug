.row
    .col-xs-12.col-sm-6
        kof-date-range((onSelect)="selectDateRange($event)")
    .col-xs-12.col-sm-6(*ngIf="reportType === reportTypes.paymentRegistry")
        label Создать реестр за этот период?:
        .row
            .col-xs-12
                button.btn.btn-default(
                    (click)="createReports()",
                    [disabled]="isLoading") Создать
.row
    .col-xs-12
        kof-search-reports-result([reports$]="reports$", [filter]="filter")
