.x_panel
    .x_content
        kof-contract-group(*ngIf="currentStep === step.contract")
        kof-payout-tool-group(*ngIf="currentStep === step.payoutTool")
        kof-shop-group(*ngIf="currentStep === step.shop")
        .row
            .col-xs-12
                button.btn.btn-default.pull-right(
                    *ngIf="currentStep !== step.shop",
                    [disabled]="!validStep",
                    (click)="next()") Далее
                button.btn.btn-primary.pull-right(
                    *ngIf="currentStep === step.shop",
                    [disabled]="!validStep",
                    (click)="createClaim()") Создать заявку