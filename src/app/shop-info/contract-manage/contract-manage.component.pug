.row(*ngIf="shop")
    .col-xs-12.col-md-8.col-md-offset-2
        .x_panel
            .x_title
                .row
                    .col-xs-6
                        h5 Выбор контракта
                    .col-xs-6
                        button.btn.btn-default.btn-sm.pull-right((click)="navigateBack()") Назад
            .x_content
                kof-contract-select([currentContractID]="shop.contractID", (onSelect)="selectContract($event)")

        div(*ngIf="selectedContract")
            .x_panel
                .x_title
                    h5 Данные контракта
                .x_content
                    kof-contract-details([contract]="selectedContract")

            .x_panel
                .x_title
                    h5 Выбор средства вывода
                .x_content
                    kof-payout-tool-select([contractID]="selectedContract.id", [currentPayoutToolID]="shop.payoutToolID", (onSelect)="selectPayoutTool($event)")

            div(*ngIf="selectedPayoutTool")
                .x_panel
                    .x_title
                        h5 Данные средства вывода
                    .x_content
                        kof-payout-tool-details([payoutTool]="selectedPayoutTool")

                .x_panel(*ngIf="selectedContract.id !== shop.contractID || selectedPayoutTool.id !== shop.payoutToolID")
                    .x_content
                        button.btn.btn-primary.pull-right((click)="bindContract()") Создать заявку на привязку данного контракта

        .x_panel(*ngIf="!selectedContract")
            .x_content
                kof-contract-create([shopID]="shop.id", (onCreate)="createAndBindContract($event)")
