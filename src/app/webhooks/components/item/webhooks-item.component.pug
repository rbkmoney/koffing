.row
    .col-xs-12
        .x_panel
            .x_title
                .row
                    .col-xs-6
                        h4 Создание нового webhook
            form.form-horizontal.form-label-left
                .form-group
                    label.control-label.col-md-3.col-sm-3.col-xs-12
                        span.required URL
                    .col-md-6.col-sm-6.col-xs-12
                        input.form-control.col-md-7.col-xs-12(type="text", id="url", name="url", [(ngModel)]="model.url")
                .form-group
                    label.control-label.col-md-3.col-sm-3.col-xs-12(for="topic")
                        span.required topic
                    .col-md-6.col-sm-6.col-xs-12
                        select.form-control.col-md-7.col-xs-12(id="topic", name="topic", [(ngModel)]="model.scope.topic")
                            option
                            option(*ngFor="let option of topics") {{option.value}}
                .form-group
                    label.control-label.col-md-3.col-sm-3.col-xs-12
                        span.required shopID
                    .col-md-6.col-sm-6.col-xs-12
                        input.form-control.col-md-7.col-xs-12(type="number", id="shopID", name="shopID", [(ngModel)]="model.scope.shopID")
                .form-group
                    label.control-label.col-md-3.col-sm-3.col-xs-12
                        span.required eventTypes
                    .col-md-6.col-sm-6.col-xs-12
                        .checkbox(*ngFor="let checkbox of eventTypes")
                            label
                                input(type="checkbox", id="{{checkbox.name}}", name="{{checkbox.name}}", [(ngModel)]="checkbox.value", (change)="onChangeEventTypes()")
                                span {{checkbox.name}}
                .form-group
                    .col-md-6.col-sm-6.col-xs-12.col-md-offset-3
                        button.btn.btn-primary((click)="toggleAll(true)") Выбрать всё
                        button.btn.btn-primary((click)="toggleAll(false)") Отменить
                button.btn.btn-success((click)="createWebhook()") Создать

