table.table.table-hover.table-striped
    thead
        tr
            th Номер
            th Сумма
            th Валюта
            th Дата
            th Статус
            th Описание
    tbody
        tr(*ngFor="let invoice of invoices")
            td {{invoice.id}}
            td {{invoice.amount | roubleCurrency}}
            td {{invoice.currency}}
            td {{invoice.dueDate | date: "dd.MM.yyyy HH:mm:ss"}}
            td {{invoice.status | paymentStatus}}
            td(width="400") {{invoice.description}}