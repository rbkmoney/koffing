.panel.panel-default
    .panel-heading((click)="show()")
        a(href="#") Создание магазина: {{shop.shopDetails.name}}
        i.fa.pull-right.custom-chevron([ngClass]="{'fa-chevron-down': showPanel, 'fa-chevron-right': !showPanel}")
    .panel-body(*ngIf="showPanel")
        div(*ngIf="shop.shopDetails")
            modification-detail([displayName]="'Название магазина'", [value]="shop.shopDetails.name")
            modification-detail([displayName]="'Описание'", [value]="shop.shopDetails.description")
            modification-detail([displayName]="'Месторасположение'", [value]="shop.shopDetails.location")
        div(*ngIf="shop.contractor")
            modification-detail([displayName]="'Наименование организации'", [value]="shop.contractor.registeredName")
            modification-detail([displayName]="'Тип юридического лица'", [value]="shop.contractor.legalEntity")
