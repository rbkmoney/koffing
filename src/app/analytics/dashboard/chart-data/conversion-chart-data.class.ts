export class ConversionChartData {
    public conversion: number;
    public offset: number;

    constructor(conversion: number, offset: number) {
        this.conversion = conversion;
        this.offset = offset;
    }
}
